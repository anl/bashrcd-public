if [ -f /etc/bash_completion ] ; then
    . /etc/bash_completion
elif [ -f /usr/local/etc/bash_completion ] ; then
    . /usr/local/etc/bash_completion
fi

# Turn on tab completion with extra information:
set show-all-if-ambiguous on
set visible-stats on
