wp_virsh ()
{
    host=$1
    default_domain=esx.pages
    echo ${host} | grep -q '\.' || host=${host}.${default_domain}
    shift
    user=kvmadmin
 
    virsh -c "qemu+ssh://${user}@${host}/system?socket=/var/run/libvirt/libvirt-sock" "$@"
}
